# Runtime dependencies for the Chorus Telegram bot (bot.py).
openai>=1.40.0        # picks which words are worth learning
python-dotenv>=1.0.1  # local .env; hosting platforms set real env vars
httpx>=0.27           # Telegram Bot API and LRCLIB calls
av>=11                # reads title/artist tags off the uploaded music file
